Roles precede tasks when both are specified.

The order basically is:

- pre_tasks
- roles
- tasks
- post_tasks

https://docs.ansible.com/ansible/latest/user_guide/playbooks_reuse_roles.html#using-roles
